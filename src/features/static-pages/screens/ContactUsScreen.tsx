import { Header, ScreenWrapper } from '@/src/shared/ui';

export function ContactUsScreen() {
  return (
    <ScreenWrapper>
      <Header title="Contact Us" subtitle="Reach the support team" />
      {/* TODO: Add contact channels and support form integration. */}
    </ScreenWrapper>
  );
}
